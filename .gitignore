# Data outputs (large, regenerated by pipeline — all live in data/)
data/papers_database.json
data/papers_ranked.csv
data/top_papers_for_review.json

# Phase 2 (cost-effectiveness)
data/shortlist.json
data/cea_by_intervention.json
data/*.csv

# Raw API responses (reproducibility archive, not tracked)
data/raw_responses/

# Python
__pycache__/
*.pyc
*.pyo
.env

# R
.Rhistory
.RData
.Rproj.user

# Harness / editor
.claude/scheduled_tasks.lock

# CEGA brand source assets — logos are embedded (base64) in the slide deck,
# so the source folder isn't needed in-repo. Heavy binaries especially.
CEGA Logos/*.mov
CEGA Logos/*.ai
CEGA Logos/EPS/

# OS
.DS_Store
