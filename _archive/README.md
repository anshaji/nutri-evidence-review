# _archive

Archived rendered artifacts (PDF / HTML) and superseded documents. Original
sub-paths are mirrored under `_archive/` so nothing is lost.

The **current, canonical sources are the Markdown files in the main tree** —
`output/FULL_INTERVENTION_SYNTH.md`, `claude.md`, `PROCESS_DOCUMENTATION.md`,
`README.md`. Re-render from those when you need fresh PDFs/HTML.

Contents:

- `PROCESS_DOCUMENTATION.html`, `output/PROCESS_DOCUMENTATION.pdf` — stale renders of the v2 methodology doc.
- `output/FULL_INTERVENTION_SYNTH.{html,pdf}` — earlier renders of the synthesis (the live `.md` has since been updated to the v3 two-phase, claim-verified version).
- `output/INTERVENTION_SYNTHESIS.{html,pdf}` — the superseded v2 abstract-only synthesis (the `.md` remains in `output/` as a historical reference).
- `output/chatgpt/` — ChatGPT rapid-review comparison run.
- `output/claude/` — Claude rapid-review comparison run and evidence-review slides.
